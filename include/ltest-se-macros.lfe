;;;; Wrappers for the Erlang Selenium webdriver library
(eval-when-compile

  (defun get-session-funcs ()
    '((execute 3)
      (execute-async 3)
      (get-screenshot 1)
      (get-status 1)
      (get-window-handle 1)
      (get-window-handles 1)
      (get-ime-available-engines 1)
      (get-ime-active-engine 1)
      (get-ime-activated 1)
      (ime-deactivate 1)
      (ime-activate 2)
      (set-frame 2)
      (set-window-focus 2)
      (close-window 1)
      (set-window-size 3)
      (set-window-size 4)
      (get-window-size 1)
      (get-window-size 2)
      (set-window-position 3)
      (set-window-position 4)
      (get-window-position 1)
      (get-window-position 2)
      (set-window-maximize 1)
      (set-window-maximize 2)
      (get-cookies 1)
      (add-cookie 2)
      (delete-cookies 1)
      (delete-cookie 2)
      (get-page-source 1)
      (get-page-title 1)
      (find-element 3)
      (find-elements 3)
      (get-active-element 1)
      (find-element-rel 4)
      (find-elements-rel 4)
      (click-element 2)
      (submit 2)
      (get-text 2)
      (send-value 3)
      (send-keys 2)
      (element-name 2)
      (clear-element 2)
      (is-selected-element 2)
      (is-enabled-element 2)
      (element-attribute 3)
      (are-elements-equal 3)
      (is-displayed-element 2)
      (get-element-info 2)
      (element-location 2)
      (is-element-location-in-view 2)
      (element-size 2)
      (element-css-property 3)
      (get-browser-orientation 1)
      (set-browser-orientation 2)
      (get-alert-text 1)
      (set-alert-text 2)
      (accept-alert 1)
      (dismiss-alert 1)
      (moveto 4)
      (click 2)
      (buttondown 2)
      (buttonup 2)
      (doubleclick 1)
      (get-log 2)
      (get-log-types 1)
      (get-cache-status 1)
      (set-timeout 3)
      (set-async-script-timeout 2)
      (set-implicit-wait-timeout 2)
      (forward 1)
      (back 1)
      (refresh 1)
      (set-url 2)
      (get-url 1)
      (start-session 3)
      (start-session 4)
      (stop-session 1)
      (stop 1)))

  (defun get-cap-funcs ()
    '((default 0)
      (default-browser 1)
      (to-json 1)
      (default-firefox 0)
      (default-chrome 0)
      (default-htmlunit 0)
      (default-safari 1)))
  ;; end of eval-when-compile
  )

(defmacro generate-session-api ()
  `(progn ,@(kla:make-funcs (get-session-funcs) 'webdrv_session)))

(defmacro generate-capabilities-api ()
  `(progn ,@(kla:make-funcs (get-cap-funcs) 'webdrv_cap)))

(generate-session-api)
(generate-capabilities-api)

(defun loaded-ltest-se-meacros ()
  "This is just a dummy function for display purposes when including from the
  REPL (the last function loaded has its name printed in stdout).
  This function needs to be the last one in this include."
  'ok)
